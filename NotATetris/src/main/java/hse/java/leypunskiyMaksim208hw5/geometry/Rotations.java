package hse.java.leypunskiyMaksim208hw5.geometry;

public enum Rotations {
    DEG0,
    DEG90,
    DEG180,
    DEG270;

    // Number of all rotations
    public static final int count = Rotations.values().length;
}
